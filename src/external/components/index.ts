"use client";

// import {
//   Button,
//   Typography,
//   Navbar,
//   Spinner,
//   Input,
// } from "@material-tailwind/react";

const Button = (props: any) => null;
const Typography = (props: any) => null;
const Navbar = (props: any) => null;
const Spinner = (props: any) => null;
const Input = (props: any) => null;

export { Button, Typography, Navbar, Spinner, Input };
